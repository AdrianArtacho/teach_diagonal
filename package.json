{
  "name": "diamond-midi-tutor",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "A dependency-free diamond Launchpad keyboard and MIDI melody tutor",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
